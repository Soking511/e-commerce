{
  "version": 2,
  "builds": [
    {
      "src": "server/api/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/api/app.ts"
    }
  ]
}